{% extends 'base.html.twig' %}
{% block content %}
    <!-- Header -->
    <div id="top-nav" class="navbar navbar-inverse navbar-static-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-toggle"></span>
                </button>
                <a class="navbar-brand" href="index.php">Pokemon Battle</a>
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav navbar-right">
                    {% if panel == false %}
                    <li>
                        <a href="login.php">login</a>
                    </li>
                    <li>
                        <a href="createTrainer.php">Create Trainer</a>
                    </li>
                    {% endif %}
                    {% if panel %}
                        <li class="dropdown">
                            <a class="dropdown-toggle" role="button" data-toggle="dropdown" href="#">
                                <i class="glyphicon glyphicon-user"></i>{{ username }}<span class="caret"></span></a>
                            <ul id="g-account-menu" class="dropdown-menu" role="menu">

                                <li><a href="logout.php"><i class="glyphicon glyphicon-lock"></i> Logout</a></li>
                            </ul>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </div><!-- /container -->
    </div>
    <!-- /Header -->

    <div class="container">
        {% if panel %}
            <div class="col-md-6">
                <a href="battle.php">
                    <img src="src/Resources/img/logo-pokemon.png" alt="Battle Pokemon" class="img-thumbnail">
                    <button type="button" class="btn btn-default btn-lg">Battle Pokemon</button>
                </a>
            </div>
            <div class="col-md-6">
                <a href="pokecenter.php">
                    <img src="src/Resources/img/Giratina.png" alt="Pokémon" class="img-thumbnail">
                    <button type="button" class="btn btn-default btn-lg">heal Pokemon</button>
                </a>
            </div>
        {% endif %}
    </div>
{% endblock content %}
